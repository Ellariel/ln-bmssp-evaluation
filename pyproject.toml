[project]
name = "ln-bmssp-evaluation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "networkx",
    "tqdm",
    "scipy",
    "pandas",
    "matplotlib",
    "ipykernel",
]
